export type NavItem = {
 label: string;
 to: string;
};
export type TeamMember = {
 id: string;
 name: string;
 role: string;
 image: string;
 github?: string;
 linkedin?: string;
};
export type SolutionFeature = {
 slug: string;
 title: string;
 description: string;
};