// Configuração do PostCSS usada pelo Tailwind CSS no build.
module.exports = {
  plugins: {
    // Processa as classes utilitárias usadas nos componentes.
    tailwindcss: {},
    // Adiciona prefixos CSS quando necessário para compatibilidade.
    autoprefixer: {},
  },
}
